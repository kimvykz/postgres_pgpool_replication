# Allow all local connections
local   all             all                                     trust
host    all             root            0.0.0.0/0               md5
host    all             all             0.0.0.0/0               md5
local    all             all                            trust
host     all             root       all         trust
host     all             root       all         scram-sha-256
host     all             all                all         scram-sha-256
local    all             all                            trust
host     all             root       all         trust
host     all             root       all         scram-sha-256
host     all             all                all         scram-sha-256
local    all             all                            trust
host     all             root       all         trust
host     all             root       all         scram-sha-256
host     all             all                all         scram-sha-256
local    all             all                            trust
host     all             root       all         trust
host     all             root       all         scram-sha-256
host     all             all                all         scram-sha-256
